spring:
  application:
    name: demo-application # 应用名

logging:
  file: /Users/yuyong/logs/${spring.application.name}.log
  file.max-history: 7 # 日志文件要保留的归档的最大天数。默认为 7 天。
  file.max-size: 10MB # 日志文件的最大大小。默认为 10MB 。

  # 日志文件配置
#  file:
#    #    path: /Users/yuyong/logs/ # 日志文件路径。
#    name: /Users/yuyong/logs/${spring.application.name}.log # 日志文件名。
#    max-history: 7 # 日志文件要保留的归档的最大天数。默认为 7 天。
#    max-size: 10MB # 日志文件的最大大小。默认为 10MB 。

  # 日志级别
  level:
    cn:
      javadog:
        labs:
          springboot2:
            logging:
              simple:
                controller: DEBUG